pipelines:
  - name: pipe_1
    steps:
      - name: without_cofiguration
        type: bash
        setup:
          runtime:
            type: as
        execution:
          onExecute:
            - echo "executing step without_cofiguration...1a"

      - name: with_cofiguration
        type: bash
        configuration:
          affinityGroup: some_group
          timeoutSeconds: 1200
          cache: true
          priority: 9876
        setup:
          runtime: onHost
        execution:
          onExecute:
            - echo "executing step with_cofiguration...1"
